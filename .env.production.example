VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
VITE_API_BASE=https://your.production.api.example.com/api

# Notes:
# - Replace the values above with your production API base and Google OAuth client ID.
# - Do NOT commit your real `.env.production` file with secrets. Use this example as a template.
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
VITE_API_BASE=https://your.production.api

# Instructions: copy this file to `.env.production` (don't commit secrets)
# and replace placeholder values with real production settings before running
# `npm run build` for production.